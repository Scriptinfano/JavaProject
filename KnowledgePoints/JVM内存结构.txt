编译完源程序之后，生成多个字节码文件，同一个文件中有多少个类就生成几个字节码文件
JVM使用类的加载器和解释器对生成的字节码文件进行解释运行，意味着需要将字节码文件对应的类加载到内存中，涉及到内存解析

  二、JVM五大内存区域
    1、虚拟机栈：即平时所提到的栈结构
    2、堆：我们将new出来的结构（数组与对象）加载在堆空间中。补充：对象的非static属性将加载在堆空间中
    3、方法区：类的加载信息，常量池，静态域


